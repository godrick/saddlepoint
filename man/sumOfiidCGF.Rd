% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/D-SumsOfiidCGF.R
\name{sumOfiidCGF}
\alias{sumOfiidCGF}
\title{Create a CGF object for the sum of \code{n} i.i.d. random variables.}
\usage{
sumOfiidCGF(cgf, n, block_size = NULL, iidReps = NULL, ...)
}
\arguments{
\item{cgf}{A \code{CGF} object describing the base random variable \eqn{X}.}

\item{n}{Either a positive scalar, or a function/adaptor mapping \eqn{\theta \mapsto n(\theta)}.}

\item{block_size}{Optional. Positive integer giving the dimension of one observation of \eqn{Y}.}

\item{iidReps}{Optional. Either \code{NULL}, \code{"any"}, or a positive integer.}

\item{...}{Additional arguments passed to CGF creation.}
}
\value{
A \code{CGF} object for \eqn{Y = \sum_{i=1}^{n(\theta)} X_i}.
}
\description{
Given a base CGF object \code{cgf} describing a random variable \eqn{X},
this function returns a new CGF object for the sum of \code{n} i.i.d. copies of \eqn{X},
i.e. \eqn{Y = X_1 + \cdots + X_n}.
}
\details{
\deqn{K_Y(t;\theta) = n(\theta)\,K_X(t;\theta).}

For every order \eqn{r \ge 1},
\deqn{K_Y^{(r)}(t;\theta) = n(\theta)\,K_X^{(r)}(t;\theta).}

\itemize{
\item \code{n} is the number of summands inside each \eqn{Y}.
\item \code{iidReps}/\code{block_size} (if supplied) describe i.i.d. replication of \eqn{Y}
across observations \eqn{Y_1,\ldots,Y_B}.
}

\strong{Replication (iidReps / block_size):}
\itemize{
\item If both \code{iidReps} and \code{block_size} are \code{NULL}, no replication is applied.
\item If \code{block_size} is provided but \code{iidReps} is \code{NULL}, we set \code{iidReps = "any"} and
infer the number of blocks from \code{length(tvec) / block_size} at evaluation time.
\item If \code{iidReps = "any"}, then \code{block_size} must be provided.
\item If \code{iidReps} is a positive integer, \code{block_size} may be \code{NULL}, though providing it is safer.
}
}
\examples{
\dontrun{
base_cgf <- NormalCGF
sum5 <- sumOfiidCGF(base_cgf, n=5)
theta <- c(1.2, 0.8)
tvec  <- c(0.1, -0.2, 0.0)

all.equal(sum5$K(tvec, theta), 5*base_cgf$K(tvec, theta))
all.equal(sum5$K1(tvec, theta), 5*base_cgf$K1(tvec, theta))
}

}
