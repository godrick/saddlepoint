% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/C-GeometricCGF.R
\name{GeometricModelCGF}
\alias{GeometricModelCGF}
\title{Create a Parametric Geometric CGF Object}
\usage{
GeometricModelCGF(prob, iidReps = "any", ...)
}
\arguments{
\item{prob}{A function (or adaptor) that accepts a single parameter vector \code{theta}
and returns the success probability \eqn{prob} (a scalar) or a vector of probabilities.}

\item{iidReps}{Either \code{"any"} (no forced dimension) or a positive integer specifying how many
i.i.d. blocks are expected. Each block correspond to one copy of the geometric variables (or multiple if \code{prob} is a vector).}

\item{...}{Additional arguments passed to the underlying CGF creation function}
}
\value{
A \code{CGF} object
}
\description{
Creates a CGF object for the Geometric distribution with success probability
\eqn{prob(\theta)} given by a user-supplied function or adaptor.
The resulting object correspond to the random variable that counts the number of failures before achieving the first success.
It supports i.i.d. and non-identical contexts with optional length enforcement via \code{iidReps}.
}
