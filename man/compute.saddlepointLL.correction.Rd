% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/B-compute-saddlepointLL-correction.R
\name{compute.saddlepointLL.correction}
\alias{compute.saddlepointLL.correction}
\title{Compute the correction term for the saddlepoint approximation to the log-likelihood}
\usage{
compute.saddlepointLL.correction(
  parameter_vector,
  observed.data,
  cgf,
  tvec.hat = NULL,
  gradient = FALSE,
  hessian = FALSE,
  spa_method = "standard",
  tvec_source = c("auto", "user", "analytic", "newton", "solver_atomic"),
  solver_fun = saddlepoint.solve,
  newton_t_init = NULL,
  ...
)
}
\arguments{
\item{parameter_vector}{Numeric vector of parameters.}

\item{observed.data}{Numeric vector of observed data.}

\item{cgf}{A \code{CGF} object.}

\item{tvec.hat}{(Optional) Numeric vector. If supplied, \code{tvec} is taken directly
as this vector (the saddlepoint \eqn{\hat{t}}). Otherwise we compute it (analytic if
available, else numeric solve).}

\item{gradient}{Logical. If \code{TRUE}, return gradient wrt \code{parameter_vector}.}

\item{hessian}{Logical. If \code{TRUE}, return Hessian wrt \code{parameter_vector}.}

\item{spa_method}{Character string. One of \code{"standard"} (first-order) or \code{"zeroth"}.}

\item{tvec_source}{One of "auto","user","analytic","newton","solver_atomic". See \code{compute.spa.negll}.}

\item{solver_fun}{Numeric solver used when \code{tvec_source="solver_atomic"} or in non-AD path. Default \code{saddlepoint.solve}.}

\item{newton_t_init}{Optional start for t in the Newton solver path.}

\item{...}{Passed to \code{solver_fun} when used.}
}
\value{
A named list with elements:
\describe{
\item{val}{Numeric scalar of the correction term.}
\item{gradient}{The gradient at \code{theta}, if \code{gradient=TRUE}.}
\item{hessian}{The Hessian at \code{theta}, if \code{hessian=TRUE}.}
}
}
\description{
This function calculates the correction term for the saddlepoint approximation
to the log-likelihood. By default, it computes that of the \strong{first-order} approximation,
If \code{spa_method="zeroth"}, it instead computes the correction term for the \strong{zeroth-order} approximation.
}
\seealso{
\code{\link{find.saddlepoint.MLE}}, \code{\link{compute.spa.negll}}
}
